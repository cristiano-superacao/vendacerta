# ⚙️ VARIÁVEIS DE AMBIENTE - RAILWAY
# Copie e cole cada linha nas "Variables" do Railway

# ========================================
# 🔐 SEGURANÇA (OBRIGATÓRIO)
# ========================================
FLASK_SECRET_KEY=${{ secret() }}
CHAVE_SECRETA=${{ secret() }}

# ========================================
# 🌍 AMBIENTE (OBRIGATÓRIO)
# ========================================
FLASK_ENV=production
FLASK_DEBUG=False
FLASK_APP=app.py

# ========================================
# 🐍 PYTHON (OBRIGATÓRIO)
# ========================================
PYTHONUNBUFFERED=1
VERSAO_DO_PYTHON=3.11

# ========================================
# 🦄 GUNICORN (OBRIGATÓRIO)
# ========================================
TEMPO_DE_TEMPO_DE_GUNICORNIO=120
CONCORRENCIA_WEB=2

# ========================================
# 🗄️ BANCO DE DADOS (CONFIGURAR)
# ========================================
# Usar a variável do Railway PostgreSQL:
URL_DO_BANCO_DE_DADOS=${DATABASE_URL}

# ========================================
# 🎛️ CONTROLE (OPCIONAL)
# ========================================
SKIP_INIT=0
SOMENTE_BANCO_DE_DADOS_INICIALIZADO=0

# ========================================
# 📊 BANCOS SEPARADOS (OPCIONAL - AVANÇADO)
# ========================================
# Só configure se precisar de bancos separados por módulo
# Por padrão, todos usam DATABASE_URL
# DATABASE_URL_AUTH=${DATABASE_URL}
# DATABASE_URL_VENDAS=${DATABASE_URL}
# DATABASE_URL_CLIENTES=${DATABASE_URL}
# DATABASE_URL_ESTOQUE=${DATABASE_URL}
# DATABASE_URL_SERVICOS=${DATABASE_URL}
# DATABASE_URL_COMUNICACAO=${DATABASE_URL}

# ========================================
# 📝 VARIÁVEIS AUTOMÁTICAS DO RAILWAY
# ========================================
# Essas são criadas AUTOMATICAMENTE quando você adiciona PostgreSQL:
# - PGHOST (gerado pelo Railway)
# - PGPORT (gerado pelo Railway)
# - PGUSER (gerado pelo Railway)
# - PGPASSWORD (gerado pelo Railway)
# - PGDATABASE (gerado pelo Railway)
# - DATABASE_URL (gerado pelo Railway)
#
# NÃO PRECISA CONFIGURAR MANUALMENTE!

# ========================================
# ✅ CHECKLIST
# ========================================
# [ ] FLASK_SECRET_KEY configurada com ${{ secret() }}
# [ ] FLASK_ENV=production
# [ ] FLASK_DEBUG=False
# [ ] PYTHONUNBUFFERED=1
# [ ] URL_DO_BANCO_DE_DADOS=${DATABASE_URL}
# [ ] PostgreSQL service criado e rodando
# [ ] Deploy realizado com sucesso
# [ ] Health check respondendo em /ping
# [ ] Login funcionando em /login
