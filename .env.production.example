# ============================================================================
# VARIÁVEIS DE AMBIENTE - PRODUÇÃO RAILWAY
# ============================================================================
# IMPORTANTE: Este arquivo é apenas um exemplo!
# No Railway, configure estas variáveis em: Settings → Variables
# NUNCA commite valores reais de SECRET_KEY ou senhas!
# ============================================================================

# ----------------------------------------------------------------------------
# 🔐 SEGURANÇA - CRÍTICO
# ----------------------------------------------------------------------------
# NÃO use ARG ou ENV no Dockerfile para estes valores!
# Configure apenas no Railway Dashboard → Variables

# Flask Secret Key (gerar com: python -c "import secrets; print(secrets.token_urlsafe(64))")
FLASK_SECRET_KEY=GERAR_NO_RAILWAY_COM_COMANDO_ACIMA

# Chave alternativa (fallback)
CHAVE_SECRETA=GERAR_NO_RAILWAY_COM_COMANDO_ACIMA

# ----------------------------------------------------------------------------
# 🌍 AMBIENTE
# ----------------------------------------------------------------------------
FLASK_ENV=production
FLASK_DEBUG=False
PORT=5000

# ----------------------------------------------------------------------------
# 🗄️ BANCO DE DADOS
# ----------------------------------------------------------------------------
# Railway fornece automaticamente DATABASE_URL ao conectar PostgreSQL
# NÃO configure manualmente - deixe o Railway gerenciar
# DATABASE_URL=postgresql://user:pass@host:5432/db (AUTO GERADO)

# Configurações de pool de conexão
SQLALCHEMY_POOL_SIZE=5
SQLALCHEMY_MAX_OVERFLOW=10
SQLALCHEMY_POOL_TIMEOUT=30
SQLALCHEMY_POOL_RECYCLE=3600

# ----------------------------------------------------------------------------
# 📝 LOGGING
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO
LOG_FORMAT=json

# ----------------------------------------------------------------------------
# 🛡️ RATE LIMITING
# ----------------------------------------------------------------------------
RATELIMIT_ENABLED=True
RATELIMIT_STORAGE_URL=memory://
RATELIMIT_DEFAULT=100 per hour

# ----------------------------------------------------------------------------
# ⚡ PERFORMANCE
# ----------------------------------------------------------------------------
ENABLE_COMPRESSION=True
ENABLE_CACHE=True
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300

# ----------------------------------------------------------------------------
# 🔒 SESSION
# ----------------------------------------------------------------------------
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax
SESSION_PERMANENT_LIFETIME=86400

# ----------------------------------------------------------------------------
# 🎨 FRONTEND
# ----------------------------------------------------------------------------
# Bootstrap e assets (não há segredos aqui)
BOOTSTRAP_SERVE_LOCAL=False
BOOTSTRAP_VERSION=5.3.3

# ----------------------------------------------------------------------------
# 📧 EMAIL (Opcional - configure se necessário)
# ----------------------------------------------------------------------------
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=seu-email@gmail.com
# MAIL_PASSWORD=USAR_APP_PASSWORD_NAO_SENHA_REAL

# ----------------------------------------------------------------------------
# 📊 MONITORING (Opcional)
# ----------------------------------------------------------------------------
# SENTRY_DSN=https://...
# SENTRY_ENVIRONMENT=production

# ============================================================================
# INSTRUÇÕES PARA CONFIGURAR NO RAILWAY
# ============================================================================
# 
# 1. Gerar SECRET_KEY segura:
#    python -c "import secrets; print(secrets.token_urlsafe(64))"
#
# 2. No Railway Dashboard:
#    - Vá em: Projeto → Serviço VendaCerta → Variables
#    - Clique em: "+ New Variable"
#    - Adicione cada variável acima (exceto comentários)
#
# 3. Variáveis automáticas do Railway:
#    - DATABASE_URL: Gerado ao conectar PostgreSQL
#    - RAILWAY_ENVIRONMENT: production
#    - RAILWAY_PROJECT_NAME: vendacerta
#    - PORT: Gerado automaticamente
#
# 4. NÃO adicione no Dockerfile:
#    ❌ ARG FLASK_SECRET_KEY
#    ❌ ENV FLASK_SECRET_KEY
#    ❌ ARG ADMIN_PASSWORD
#    ❌ ENV SECRET_KEY
#
# 5. Use Railway Secrets (recomendado):
#    - Para dados ultra-sensíveis
#    - Criptografados em repouso
#    - Não aparecem em logs
#
# ============================================================================
